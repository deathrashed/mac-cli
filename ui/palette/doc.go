package palette

// Package palette is reserved for the command-palette model as the TUI grows.
// The first implementation shares the Bubbles list with ui/mainmenu so command
// filtering stays simple and testable.
